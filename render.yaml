services:
  - type: web
    name: budgets-proxy
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.mjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    envVarGroups:
      - envVarGroup: secrets
